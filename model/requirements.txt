# Machine Learning
# Install PyTorch separately using: 
# CPU version: pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cpu
# GPU version (CUDA 12.1): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
numpy
scikit-learn
pandas

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Security
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
passlib==1.7.4
python-dotenv==1.2.2
slowapi==0.1.9

# Utilities
scipy
matplotlib
Pillow
tqdm==4.67.3

# Video Processing
opencv-python

# Pose Estimation
Cython>=0.29.0
mediapipe>=0.10.0
simple-romp  # use PyPI package 'simple-romp'

# Monitoring
prometheus-client==0.19.0

# Testing
pytest==7.4.3
httpx==0.27.0

# Pydantic settings
pydantic-settings

# ===================================================================
# Therapy Module (RAG System) Dependencies
# ===================================================================

# Core LangChain
langchain
langchain-community
langchain-huggingface
langchain-text-splitters

# PDF loading — IBM Docling (layout-aware, OCR, token-aligned chunking)
docling

# Vector store — ChromaDB (local)
chromadb

# Embeddings — local sentence-transformers
sentence-transformers

# BM25 sparse retrieval
rank-bm25

# NLTK — tokenisation + lemmatisation for BM25
nltk==3.9.3

# LLM generation — Gemini via OpenAI-compatible endpoint
openai==2.26.0
google-generativeai==0.8.6